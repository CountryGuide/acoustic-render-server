import React from 'react';

const Home = () => {
    return (
        <div>
            <div>Test nodemon</div>
            <button onClick={() => console.log('test')}>Press!</button>
        </div>

    );
};

export default Home;